c_coords();

// control -----

waveTime = 800;
nextWave = o_wave_boss;

alarm[0] = waveTime;

// actions --------

c_enemy_spawn(xder, y1, o_capibara_n)

c_enemy_spawn(xder, y3, o_capibara_n)

c_enemy_spawn(xizq, y2, o_capibara_b)
